# Includes PHP

Funciones PHP compartidas.

- `store-config.php`: sesiones, normalizacion de productos, persistencia en MySQL/JSON y utilidades del catalogo.

La configuracion de base de datos vive en `config/database.php`.
