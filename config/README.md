# Configuracion

Archivos de configuracion del proyecto:

- `database.php`: conexion MySQL usada por la tienda y el panel administrativo.

El resto de funciones PHP compartidas vive en `includes/store-config.php`.
